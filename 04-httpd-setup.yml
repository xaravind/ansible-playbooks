---
- name: Update all servers
  hosts: all

  tasks:
  - name: Ensure httpd installed
    package:
      name: httpd
      state: present

  - name: Ensure httpd started
    service:
      name: httpd
      state: started
      enabled: true

